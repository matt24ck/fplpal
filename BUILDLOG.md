# Build Log

Short record of what was implemented at each step, in order. Companion to [PLAN.md](PLAN.md) (the "what and why") — this is the "what actually got built".

## 1. Plans

**[PLAN.md](PLAN.md)** — product and architecture plan: bottom-up modeling stack (team strength → minutes → event rates → points assembly), position-specific rating systems, MILP optimization, and a grounded chat layer where the LLM only narrates engine output. Roadmap compressed around the hard mid-August GW1 deadline. **[UI_PLAN.md](UI_PLAN.md)** — interface plan around "The Board": the pitch view as the primary analytical instrument, chips as status card + season timeline, chat that cross-highlights the data views it cites.

## 2. Project scaffold

`pyproject.toml` (Python 3.12+, httpx/pydantic/pandas/pyarrow/scipy, ruff + pytest), venv, `.gitignore` (data and build artifacts excluded), `README.md` quickstart, and the `engine/` package layout separating ingest / config / features / models.

## 3. Season rules config — `engine/config/`

All FPL game rules as versioned, pydantic-validated JSON per season (`seasons/2026-27.json`): scoring table (including 2025/26's defensive-contribution points), squad composition, formation bounds, transfer banking, chip counts. Nothing downstream hardcodes a rule. Flagged `verified_against_game: false` until it can be checked against the launched 2026/27 game.

## 4. FPL API client — `engine/ingest/fpl_api.py`

Thin httpx client for the public FPL endpoints (bootstrap-static, fixtures, element-summary, live GW, entry/picks, set-piece notes) with retry on transient failures and a dedicated `MaintenanceError` for the API's 503 "Game Updating" state. No response validation here by design — bytes get archived first, parsed later.

## 5. Snapshot archive — `engine/ingest/snapshot.py`

Every API pull saved as timestamped gzipped JSON under `data/snapshots/<endpoint>/`, so all downstream tables are rebuildable and API schema drift can never lose data. Includes a `--watch-minutes` mode that polls until the API comes back and archives the first snapshot — set up to catch the 2026/27 launch moment. Running it revealed the API is currently in summer-rollover maintenance.

## 6. Historical downloader — `engine/ingest/historical.py`

Fetches the vaastav community archive (the only per-GW source for past seasons — the official API keeps per-GW data for the current season only) over plain HTTPS, idempotently: 10 seasons (2016/17–2025/26) of `merged_gw.csv`, `players_raw.csv`, `teams.csv`, `fixtures.csv`, plus `master_team_list.csv` for early-season team names. ~45 MB total.

## 7. Canonical player-GW table — `engine/features/historical_gw.py`

Normalizes ten drifting CSV schemas into one 253,509-row parquet (one row per player per fixture). Handles: latin-1 encoding and `First_Last`/`Name_123` name formats in early seasons, positions joined from `players_raw` where absent, team identity derived from fixture opponent-pairs for seasons with no team column, assistant-manager rows dropped (2024/25), nullable xG (2022/23+) and defensive-count (2016–19, 2025/26) columns. Validated by coverage checks and by reproducing every season's real top scorer exactly (Salah 303, Haaland 272, Palmer 244, …).

## 8. Match results table — `engine/features/matches.py`

Collapses the player table into one row per match: home/away, final score, kickoff, and per-side team xG (summed player xG). 3,799 matches; every season a complete 380 except one scoreless row in the 2019/20 source data.

## 9. Team-strength model — `engine/models/team_strength.py`

First model of the stack (PLAN.md Layer 1): a Dixon-Coles Poisson model with exponential time decay, fitted by weighted MLE with analytic gradients. The response blends team xG with actual goals where xG exists; a ridge penalty stabilizes the fit; the low-score correction (rho) is fitted in a second stage on real scorelines; `as_of` fitting supports point-in-time backtests; unknown (newly promoted) teams forecast from a low-percentile prior. Outputs per fixture: expected goals both sides, full scoreline grid, clean-sheet probabilities.

**Holdout result** (fit frozen at the start of 2025/26, scored on all 380 matches): clean-sheet Brier 0.1788 vs 0.1901 constant-rate baseline; goal calibration close (2.87 predicted vs 2.75 actual per match); fitted table passes the eye test (Liverpool top attack, Arsenal top defence, home advantage ≈ +16% goals).

## 10. Minutes model — `engine/models/minutes.py`

PLAN.md Layer 2, the highest-leverage component. Three LightGBM models over strictly point-in-time features (shifted rolling and time-decayed start share/minutes, unused streak, rest days, price momentum, prev-season aggregates): a 3-class {unused, cameo, start} classifier, P(60+ | start), and E[minutes | start]; combined into `p_start` / `p_cameo` / `p_60plus` / `e_minutes` per player-fixture. GW1 cold start works by construction — in-season features are NaN and prediction leans on prev-season priors, price, and position, a shape the model also sees in training. Live injury flags (absent from historical data) overlay multiplicatively via `apply_availability`.

Two data findings along the way: the archive's 2022-23 GW1–15 have `starts` erroneously all-zero (repaired by masking any season-GW recording far fewer than 22 starters per fixture), and player names drift across seasons — so FPL's stable `code` was added to the canonical player-GW table (`historical_gw.py`) as the cross-season identity key. That fixed e.g. David Raya's GW1 forecast (name-joined prior missed → p_start 0.57; code-joined → 0.96).

**Holdout result** (trained 2022-23…2024-25, scored on all 29,747 player-fixtures of 2025-26): start Brier 0.0774 vs 0.1030 last-5-start-share baseline; minutes RMSE 21.6 vs 24.7 last-5-average baseline; decile calibration close (worst bin 0.949 predicted vs 0.930 observed); cold-start slice (GW1–5) Brier 0.0950 vs 0.1273. Deps added: lightgbm, scikit-learn.

## 11. Player event rates — `engine/models/event_rates.py`

PLAN.md Layer 3. Strictly pre-match per-90 rates per player-fixture from exponentially time-decayed career sums (grouped by the stable `code`, so rates carry across seasons), shrunk empirical-Bayes style toward position × price-quintile priors: `rate = (decayed_count + k·prior) / (decayed_90s + k)`. Covers attacking (`xg90`/`xa90` with finishing and assist multipliers), defensive-contribution counts (`cbit90`/`cbirt90` → P(threshold) via negative binomial with per-position overdispersion fitted from full-match rows, thresholds from season config), GK saves, cards, rare-event base rates, and "bonus magnetism" (`bps_res90` — per-90 residual vs a per-position BPS-from-events regression). Each stat family keeps its own decayed exposure because coverage differs by era (xG 2022-23+; DC counts 2016-19 and 2025-26 only).

Findings: the archive's `defensive_contribution` column is the raw CBIT/CBIRT count, not points (verified exactly). FPL's assist definition runs far above xA (FWD ~2.5×) — so assist rates shrink toward the position's league assists/xA ratio, which fixed a −30% under-prediction of total assists. The analogous goals/xG position ratios were tried and **reverted**: they didn't generalize on holdout (totals went from +5% to +12% over), so finishing shrinks toward 1 and xG's own scale is trusted.

**Holdout result** (priors fitted before 2025-26, scored on its 11,492 played player-fixtures): P(anytime goal) Brier 0.0663 vs 0.0678 position-prior baseline; P(anytime assist) 0.0685 vs 0.0700; DC-threshold Brier 0.0870 vs 0.1338 position base rate with clean reliability; bonus magnetism persists out-of-sample (r 0.43); rate leaders pass the eye test (Haaland top xG, Cherki/Bruno top xA, Senesi/Mavropanos top DC).

## 12. Points assembly — `engine/models/points.py`

PLAN.md Layer 4: `PointsAssembler` combines the three fitted layers into per-player-fixture expected points, decomposed into named components (appearance, goals, assists, CS, conceded, saves, DC, bonus, cards, other) with a per-row variance estimate for captaincy/ceiling advice. Every scoring value comes from season config. Attacking rates scale by fixture difficulty (λ_fixture / team baseline λ, via new `baseline_lambda`/`league_lambda` on the team model); GK saves scale by opponent attack; saves/DC condition on starting (minutes model now also exposes `e_min_given_start`). Bonus = E[BPS|plays] from the BPS-events regression applied to conditional-on-playing expectations + bonus-magnetism, allocated within each fixture by an exact Plackett-Luce top-3 (O(n²) identity), so match bonus sums to 6. `aggregate_gw` collapses double gameweeks.

Two bugs caught by the eval: feeding the BPS regression *unconditional* expectations handed its intercept to benchwarmers whose weights then exploded through the play-gating division (bonus corr −0.05 → +0.27 after conditioning), and a "normalized remainder" P(3rd) approximation gave weak players more third-place probability than strong ones (replaced with the exact formula). Also diagnosed why naive form narrowly wins all-rows Spearman (0.727 vs 0.721): never-playing players form actual-points tie groups that a hard-0 prediction matches perfectly — breaking form's ties drops it to 0.652. The decision-relevant metrics are unambiguous.

**Holdout result** (2025-26 replayed point-in-time, team strength refit before every GW, 29,747 player-fixtures): total xPts +1.3% of actual; every component total within ~6% of realized; RMSE 1.89 vs 2.13 (last-4 form) and 2.06 (season PPG); played-only rank corr 0.374 vs 0.274/0.290; top-10 overlap 0.336 vs 0.302/0.294. Season xPts leaders are the real elite (Haaland 223 projected / 239 realized); known miss: Salah projected 176 vs 123 realized — the 270-day rate half-life smooths his decline, a knob for the backtest harness.

## 13. Position ratings — `engine/models/ratings.py`

PLAN.md's position rating systems: composite 0-100 ratings from position-specific sub-scores (GKP: clean sheets · saves · bonus · value · minutes; DEF adds attacking threat and DC floor; MID/FWD: attacking · involvement · floor · explosiveness · value · minutes). Sub-scores are percentiles of Layer-4 projections summed over a horizon window (default next 6 GWs — fixture-adjusted and DGW-aware by construction), anchored on the relevant pool (≥45 projected mean minutes) so benchwarmers score near 0 instead of compressing the scale. Headline weights fitted per position by NNLS of realized window points on sub-scores — regression, not hand-tuned — normalized to keep the 0-100 scale. "Involvement" = share of team's projected goal+assist points (proxies focal-player/penalty status until live set-piece notes arrive); "explosiveness" = window projection sigma (ceiling). Also refactored the season replay out of `points.evaluate_holdout` into a reusable `replay_season()`.

**Holdout result** (2025-26 replay; weights fit on first-half windows, scored on second-half): rating-vs-realized rank corr beats raw xPts for GKP (0.45 vs 0.40), MID (0.58 vs 0.57), FWD (0.55 vs 0.53), ties DEF (0.60 vs 0.62). NNLS zeroes some collinear sub-scores (saves for GKP, explosiveness) on this small fit — refitting across seasons is harness work. The GW1 eye test is the headline: top pre-season MID pick was Semenyo (realized 202 points, the season's breakout), FWD Haaland 100, and the DEF sub-score table surfaces exactly the "why" the product promises (van Dijk 91 = CS 99 · DC floor 99 · bonus 98 · minutes 98).

## 14. Squad optimizer — `engine/optimize/squad.py`

PLAN.md §5 at GW1 scope: a MILP (HiGHS via PuLP, CBC fallback) that jointly picks the 15, starting XI, formation, captain, and vice. Objective = XI xPts + captain doubling + a small vice term (P(captain misses)) + bench xPts weighted by position-level autosub likelihood. Constraints all from season config: 2/5/5/3 shape, budget, ≤3 per club, formation bounds. Supports locked/excluded players ("build around X" / "never Y") and a fixed-squad mode — `optimize_lineup` is the "rate my draft" primitive (best XI / captain / bench order from any 15). Solves in ~0.4-1.9s at full-pool scale (~690 candidates). Transfer planner and chip advisor remain in-season deliverables per the roadmap.

**Holdout result** (2025-26 GW1 draft from pre-season projections, realized points over GWs 1-6 with fixed XI + captain, no transfers — symmetric across variants): **model draft 432 pts vs naive last-season-points draft 217 pts**, with the hindsight-optimal upper bound at 528 — the model's pre-season squad doubled the naive drafter and captured 82% of the ceiling. The drafted squad is a credible 2025-26 template (Raya; van Dijk/Gabriel/Tarkowski; Mbeumo/Bruno/Semenyo/Gakpo; Haaland (C), João Pedro), while the naive draft walks into the classic traps (declining Salah as captain, regressing Wood/Wissa). Constraint validation and lineup-mode reproduction both pass in the eval.

---

**Engine complete at GW1 scope.**

## 15. Live bridge — `engine/pipeline.py`, `engine/config/verify.py`

The 2026/27 API went live (checked July 23; GW1 deadline 2026-08-21) and the first snapshots are archived. `engine/config/verify.py` checks every API-verifiable rule against the season config — all 22 pass (budget, squad shape, formation bounds, club limit, banked-transfer cap, chip counts and half-windows); scoring point values aren't in the API and remain a manual check before flipping `verified_against_game`.

`engine/pipeline.py` is the nightly-refresh core: latest bootstrap + fixtures snapshots → future player-fixture rows in the canonical player-GW shape (the exact cold-start shape the models were trained on) → minutes/rates/team-strength → assembly → per-GW projections, position ratings (blend weights cached from a 2025-26 replay fit), and the optimal GW1 draft, written to `data/live/` with provenance columns (`data_snapshot`, `computed_at`). Details that mattered: future-GW features are computed one GW at a time so phantom future rows never pollute each other's history; live status flags overlay minutes (injured/suspended → 0, doubtful → chance-of-playing); team-name drift is aliased ("Ipswich Town" → "Ipswich"); and `strengths_for` now falls back to the promoted prior for *data-starved* teams, not just unknown ones — Hull's last PL data (2016-17) had decayed to ridge-average, which would have flattered them (promoted 2026/27: Coventry, Hull, Ipswich). Holdout evals re-run unchanged after these edits.

First live output (555 players, 84 new-to-league on pure priors, 23 zeroed by flags): the market moves are in — Mbeumo and Semenyo at their new clubs, Bruno Fernandes top projected at £12.0m — and the optimizer produces a £100.0m GW1 draft in ~0.4s.

## 16. API + grounded chat — `api/`, `backtest/grounding_evals.py`

The product backend (PLAN.md §6/§7). `api/data.py` loads the pipeline's `data/live/` parquets (read-only in the request path; on-demand MILP solves are the only compute); `api/tools.py` is the typed tool surface at GW1 scope — get_player, project_points (with component decomposition + ceilings), compare_players, rank_players, get_fixtures (model difficulty), explain_rating, build_squad, rate_my_draft, plus honest not-yet-available stubs for transfer/chip advice — every payload carrying provenance (`data_snapshot`, `computed_at`). `api/app.py` (FastAPI) serves the same functions as REST so UI tables and chat answers can never disagree, plus `POST /chat` streaming SSE. `api/chat.py` runs the Claude tool loop via the SDK's beta tool runner — `claude-sonnet-5` per PLAN §6, `@beta_tool`-typed tools, the grounding contract as a cached system prompt, and `tool_use`/`tool_result` SSE events so the UI can render provenance cards next to the prose. `backtest/grounding_evals.py` is the adversarial suite (guess-baiting, invented players, departed players, engine-vs-general-knowledge, squad requests must route to the MILP) with heuristic trace/text checks.

Findings: name resolution needed whole-token-before-substring matching ("Saka" was hitting Sakamoto), and Mohamed Salah genuinely left the league in summer 2026 — the tools' honest "not in the 2026-27 pool" is exactly the grounded behavior the evals now pin. All REST endpoints verified via TestClient (player cards, decompositions, fixture difficulty, squad solve with locks, draft rating round-trip, 404s and validation). The chat loop and evals are built but **not yet live-tested — no Anthropic credentials in this environment**; first run needs `ANTHROPIC_API_KEY` (then: `python -m backtest.grounding_evals`).

## 17. The Board — `web/` (Next.js UI at GW1 launch scope)

UI_PLAN.md §12's launch list, working end-to-end against the live API. Next.js 15 + React 19 + Tailwind v4 + TanStack Query, hand-scaffolded (no create-next-app); the FastAPI engine is reached through a same-origin rewrite (`/api/engine/*`), so CORS never enters the picture. Three UI-facing endpoints were added to `api/app.py` first — `/meta` (provenance + next deadline from the latest bootstrap snapshot + squad/chip rules from season config, so the client never hardcodes a rule or date), `/explorer` (player table with sub-scores + per-team fixture ticker), `/fixtures-matrix` — all verified via TestClient.

Screens: **My Team** (onboarding with draft/team-ID paths → the pitch board with xPts/fixtures overlays, GW/next-6 horizon toggle, This Week panel with live deadline countdown, chips status card), **Squad Builder** (edit-mode board, budget/rules bar with inline never-silently-blocked violations, position-filtered picker sorted by rating, MILP optimize-around-my-picks, rate-my-draft verdict card with gap-to-optimal and weakest starter), **Players** (position tabs with per-position sub-score columns, sortable, fixture tickers, player drawer: rating dial, sub-score bars, per-GW xPts decomposition, fixture run), **Fixtures** (team × GW matrix on model difficulty), **Planner** (honest pre-season state), **Chat** (desktop rail + mobile center tab: SSE streaming, typed tool cards incl. a mini-pitch for squad solutions with "use as my draft", provenance footers, contextual suggested prompts). Grounding surfaces per UI_PLAN §6: decimal/£ numbers in assistant prose render as **data chips** linked to the tool card they came from, and tool payload player names **cross-highlight on the board** (pulse ring, others dimmed) on hover and on reply completion.

Design system as specced ("tactics room, match day"): §7 tokens as CSS custom properties (`@theme static` — Tailwind v4 prunes vars referenced only from inline styles otherwise, which silently blanked the difficulty ramp until screenshots caught it), Archivo variable-width for hero/chip voices, IBM Plex Mono for all numerals. The dataviz skill's validator was run on every palette: the xPts decomposition palette passes all six checks in its stack-adjacency order (incl. the GK chain), and the difficulty ramp is a diverging pair (per-pole ordinal-validated) kept luminance-monotonic so it survives grayscale; number + color everywhere.

Verified by driving the real app with Playwright (system Edge): optimize → 15 chips on the board → rate verdict → My Team XI with captain/vice badges and bench flags → drawer decomposition → matrix, plus mobile viewports; screenshots eyeballed per the dataviz render-and-look step. That pass caught two real bugs beyond the ramp: 5-across chip rows overlapped enough to paint over the captain badge (chip width now derives from row density), and a missing `ANTHROPIC_API_KEY` killed the chat stream with no event because the SDK raises `TypeError`, not `APIError` (`chat_stream` now catches broadly and emits the SSE error event; the UI shows it as an honest state).

## 18. Chat goes live — grounding evals pass 7/7

`ANTHROPIC_API_KEY` arrived (now read from a gitignored repo-root `.env` via python-dotenv, loaded in `api/chat.py` so the server and the evals CLI both pick it up). **All 7 grounding evals pass** on the first live run: engine-grounded player answers, refusal to freelance numbers, invented players not hallucinated, departed players (Salah) reported honestly, general-knowledge answers explicitly de-badged from engine output, squad requests routed to the MILP, chip questions answered with the honest not-yet-available state.

The browser end-to-end (Playwright driving the real UI) confirmed the full grounded-chat loop live: "Build me a £100m squad" → build_squad tool card with mini-pitch, bench, and "Use as my draft" → ~30 data chips in the prose, each linked to its source card → 6 players cross-highlighted on the board behind the rail. Sonnet formats squads as markdown tables about half the time, which the minimal chat renderer flattened into pipe-soup — MiniMarkdown now renders pipe tables (separator-row detection tolerates the model attaching a bold heading to the table block), verified against a captured raw stream. One tooling gotcha worth remembering: Git Bash curl mangles `£` to Latin-1, causing 400s that look like an app bug — test with UTF-8 body files.

## 19. Deployment prep — Vercel + Railway ([DEPLOY.md](DEPLOY.md))

The repo side of going live, verified locally. `api/jobs.py` (APScheduler per PLAN §7) makes one Railway service own everything: on boot with `JOBS_ENABLED=1` it idempotently seeds an empty volume (historical download → feature builds → first snapshot → first pipeline) then schedules the hourly snapshot and nightly pipeline+reload in-process — which sidesteps Railway's one-volume-per-service constraint entirely. App hardening for a public URL: CORS origins from env, `/health` resilient during first-boot seeding (Railway healthcheck passes while the volume fills), and a per-IP hourly rate limit on `/chat` that 429s before any Anthropic spend. The web client's chat calls go direct to Railway via `NEXT_PUBLIC_CHAT_API_URL` (Vercel's rewrite proxy times out on 60–150s SSE streams); REST stays on the same-origin rewrite. `railway.json` + `.python-version` pin the build; the 620-byte rating-weights cache is now the one committed exception in the gitignored `data/` tree, saving a deployed first boot from refitting weights via a full 2025-26 replay. Verified: boot with jobs off and on, seed no-op on existing data, limit-0 chat 429s, `next build` clean.

## 20. Deployed — and an About page that proves the pitch

**The site is live**: engine on Railway (volume-seeded on first boot in ~6 min, hourly snapshot + nightly pipeline scheduled in-process), web on Vercel. Deploy war stories, for the record: Vercel initially deployed the repo root and its Python runtime seized our `api/` directory (its serverless-functions convention) — LightGBM then failed on a missing `libgomp` inside a Lambda; fixed by Root Directory = `web` plus flipping the stuck "FastAPI" framework preset to Next.js. Chat's CORS preflight 400'd because the Railway variable was named `CORS_ORIGIN` (singular); the parser now also tolerates spaces and trailing slashes. First live verification passed end-to-end: status bar provenance through the rewrite, tables through the proxy, chat streaming direct to Railway.

`/about` ("How it works") is the marketing surface for the grounding architecture: chatbot-vs-engine contrast cards, the four modeling layers with the assembly formula, the honesty mechanics, and the 2025/26 replay numbers (432 vs 217 vs 528 hindsight ceiling). Its centerpiece holds the page to the product's own standard — a **live** decomposition of the engine's current top-projected player, fetched with provenance on every view, with an offline state that says "this page would rather show you nothing than a made-up chart." Linked from the desktop nav, the onboarding hero, and the chat empty state (mobile path).

## 21. Decision-level benchmarks: the Scout head-to-head and a simulated season

Two experiments against known 2025/26 results (scripts in gitignored `test/` — scratch quality, findings recorded here). **Scout comparison**: all 38 official premierleague.com Scout Selection XIs were scraped (agents + name-matching that had to disambiguate three Arsenal Gabriels by printed position) and scored against the engine's point-in-time free XI per GW. The Scout won 2,365–2,202 (25–13 weekly), captains 7.2 vs 5.4/GW. Two known causes dominate: the replay is blind to team news (no historical injury flags — the live product overlays them) and the Salah-decay captaincy error (GW8/28/30 armbands worth 2, 2, 1 vs alternatives of 13, 13, 10). **Bot-manager simulation**: the engine played 2025/26 as a legal manager (£100m, real per-GW prices, FT banking, one −4, lineup optimizer weekly, autosubs/vice, *no chips*, news-blind, deliberately myopic single-GW transfers): **2,020 points vs the real 13.1M-manager average of 1,895**. Notable: the transfer market self-corrected Salah (sold GW16) even while captaincy kept misfiring — and the myopic churn (van Dijk→Gabriel→van Dijk in 4 GWs) is exactly what the multi-GW planner should eliminate, giving it a concrete acceptance test: beat 2,020. Both experiments sharpen the same priorities: rate-decay half-life tuning, ceiling-aware captaincy, and the planner.

## 22. Accounts — Clerk gates the surfaces that cost money

Create-account/login shipped ahead of pricing, on **Clerk** (chosen for Clerk Billing — the Stripe-backed subscription layer that makes the pricing step nearly turnkey), Google one-tap as the only sign-in method. The split: **signed-in** for Pal (chat everywhere — page, rail, AskPal bars), the Planner, and optimizer runs; **free** for the builder pitch + rate-my-draft, Players, Fixtures, About. Enforcement is server-side, not cosmetic: `api/auth.py` verifies Clerk session JWTs against the instance JWKS (PyJWT with cached keys — no per-request network hop) as a FastAPI dependency on `/chat` and `/squad/optimize`, failing **closed** with a 503 when `CLERK_ISSUER` is unset (a misconfigured deploy must not serve Anthropic spend unauthenticated; `AUTH_DISABLED=1` is the local-dev escape hatch), and the chat rate limit now counts per user id instead of per IP.

Web side: `clerkMiddleware` protects `/chat` and `/planner` (redirecting to in-app `/sign-in`), the masthead gets a sign-in pill / user button, and the rail's composer swaps to a sign-in CTA when signed out. The default (non-dynamic) `ClerkProvider` keeps auth resolution client-side, so every public page kept its static/ISR rendering — confirmed in the build output. Two flow details worth the effort: a prompt typed while signed out is queued in `sessionStorage`, so it survives the OAuth round-trip and sends itself the moment sign-in completes (desktop gets a modal over the open rail; mobile routes through the protected `/chat` and back); and suggestion chips queue the same way. One version gotcha: `@clerk/nextjs` v7 removed `SignedIn`/`SignedOut` (replaced by `Show`) — the client components use `useAuth()` conditionals instead.

Production Clerk landed the same day: DNS CNAMEs on GoDaddy (`clerk.` frontend API, `accounts.`, DKIM pair, `clkmail`), own Google OAuth client (dev instances ride Clerk's shared one; production may not), `pk_live`/`sk_live` on Vercel, `CLERK_ISSUER=https://clerk.fplpal.com` on Railway. Debug war story for the pattern library: "no sign-in button + 401s" looked like a Clerk misconfiguration but `/.well-known/jwks.json` and `/v1/environment` proved the instance healthy — the deployed site was simply a redeploy of the pre-auth commit. Check the build before the config.

## 23. Postgres — the draft follows the account, not the browser

First cross-device complaint arrived on day one (draft built on the phone, gone on the PC) — localStorage was the by-design MVP scope, accounts made its limits visible. Rather than a Clerk-metadata stopgap, Postgres per PLAN §7: a Railway database next to the engine, `psycopg` pool in `api/db.py`, one `user_state` table (Clerk user id → draft jsonb + team_id) created lazily on first use — no migration framework for one table. `GET`/`PUT /me/state` sit behind the same `require_user` dependency as chat; `DATABASE_URL` unset → 503, and the web app quietly stays local-only (same graceful-degradation posture as the engine's seeding state).

Client-side sync lives in the store: on sign-in, pull the server copy and merge — a non-empty server draft wins (it's the cross-device truth), anything only-local survives and is pushed back up; after that, edits debounce-push (800 ms) with a `lastSynced` snapshot guard so the merge itself never echoes a redundant write. Signed-out use is unchanged (localStorage), and every failure path degrades to exactly the pre-Postgres behavior.

## 24. Multi-GW transfer planner — built, backtested, shipped end-to-end

TODO §2's headline in-season feature, landed ahead of GW1. `engine/optimize/transfers.py` is a multi-period MILP over the projection window: squad evolution `sq[i,t] = sq[i,t-1] + buy - sell` with shape/club/budget-flow constraints, the FPL sell-price rule (owned players sell at purchase + half the profit; in-plan buys resell at cost; an initially-owned player can't be re-bought after selling, or his elevated sell value would survive the round trip), and the free-transfer state `f[t+1] = min(5, max(f−n,0)+1)` linearized as `f[t+1] ≤ f[t] − n[t] + h[t] + 1`, `h[t] ≥ n[t] − f[t]` — safe because hits are penalized and banked FTs are only ever useful, so the solver can't game either slack. Objective: per-GW XI + captain (+ vice/bench weights from the squad MILP), discounted γ^t, minus 4·hits. Pool restriction (owned + top-25 per position by discounted horizon xPts + 5 cheapest enablers) keeps a 6-GW plan at ~150 players ≈ 6 s with HiGHS.

**The backtest needed honest inputs**: `replay_season_frozen` (engine/models/points.py) projects every decision window using only pre-decision information — future rows blanked to the cold-start shape exactly like the live pipeline, minutes features built one target GW at a time (and one fixture-rank at a time: in a DGW, the second fixture must not see the first's phantom row), rates batched per decision (blanked rows provably contribute zero to the decayed sums), team strength as of the decision date. Validation: decision-week rows match the myopic replay at mean |Δ| 0.003 xPts, corr 0.9992; every outlier is a DGW row where the myopic frame legitimately knew the first fixture's result and the frozen frame correctly doesn't.

**Acceptance: passed.** `backtest/planner_sim.py` replays 2025/26 under full rules (sell prices, autosubs, vice, per-GW prices, news-blind, no chips): **2,152 pts at the default h=6/γ=0.9 vs the myopic bot's 2,020** (+132) and the real 13.1M-manager average of 1,895 — while paying the sell-price penalty the bot skipped. Every sweep config beat the bot (2,053–2,152): γ=1.0 overtrades hits (17 of them, 2,059), γ≤0.85 gets conservative, horizon 8 adds more far-week noise than signal. γ=0.9 is a single-season fit — the multi-season harness should revisit. Behavior worth recording: the planner sold the declining Salah at GW9 (the myopic bot held to GW16; the 2025/26 Scout comparison blamed exactly this), spent its 6 hits on pivotal restructures (Haaland in at GW9, the blank/double pivots at GW27/32), and captained Haaland 14 weeks.

Three real bugs surfaced. (1) `optimize_lineup` inherited the £100 m budget constraint, so a fixed 15 whose market value drifted past £100 m was *infeasible* — lineup choice has no budget dimension (bot_sim had dodged this with a `budget=100_000` hack). (2) Same shape with the club limit: a mid-season club move can legally leave an owned squad 4-from-one-club; the ≤3 rule is transactional, so `fix_squad` now skips it. (3) The sim harness double-granted FTs (top-of-loop +1 *and* the recursion's +1) — the first "passing" run made 73 free transfers, which 37 weekly grants cannot fund; corrected accounting (43 moves = 37 grants + 6 hits) is what the numbers above report. The lesson repeated from §22: check the arithmetic identity (moves ≤ grants + hits) before believing a headline number.

**Product wiring**: `plan_transfers` in `api/tools.py` (resolves 15 names, solves at 10 s limit, prices alternatives — "hold instead" / "best move now" / "a different move" — via no-good cuts and first-week forcing constraints, ~9 s without alternatives, ~19 s with), `POST /transfers/plan` behind `require_user` like the squad optimizer, the chat tool replacing the `transfer_advice` stub (grounding evals extended to 8 cases, 8/8 live pass — Pal asks for the user's 15 before judging a hit), a typed chat ToolCard, and the Planner page rebuilt: this-week verdict card, week-by-week timeline, alternatives with deltas, provenance footer. Pre-season the payload carries an honest note (transfers are unlimited until the GW1 deadline — the plan treats the draft as locked from GW1); after real team import (TODO §2) the same endpoint takes live squad/bank/FT/purchase-price state. `next build` clean; Planner UI verified at build level only — signed-in surfaces need a manual pass (Google one-tap can't be driven headlessly).

## 25. Chip advisor — every chip priced per week, and the sim finally plays with a full deck

The fourth optimizer (PLAN §5), built the day after the planner because the planner's trajectory is its foundation: `engine/optimize/chips.py` prices each chip per visible GW *on top of the planned squad that week*, not the current one (a bench boost is worth more the week after a wildcard builds the bench). Triple captain = the planned captain's xPts (the extra armband multiple, DGWs already in per-GW xPts); bench boost = planned bench xPts net of the autosub credit those players earn anyway; free hit = best single-week squad at full current value (one MILP solve) minus the planned XI; wildcard = PLAN's "(optimal-from-scratch, held to window end) minus what the plan achieves anyway" via a scratch solve + per-week re-lineups. Every payload carries the honesty note that gains cover only the visible window — "no standout week visible" is a real answer, and the assessment bands are engine-side so chat never invents a verdict.

**The chip-enabled replay closes the biggest disclosed gap in §24's number.** Sim policy: play early only when EV clears a per-chip threshold at the best visible week within the half; force-play before the half expires (a >0 EV chip is never worth losing); one chip per GW; WC/FH replace that week's transfers and preserve banked FTs; TC triples through the vice fallback; BB counts all 15. Result: **2,237 points — +85 over the chipless 2,152**, all eight chips played (one of each per half, GW19 split). Against the real 13.1M-manager average of 1,895 — which *includes* chips — the comparison is now like-for-like except news-blindness. The chip log reads like a coach's season: FH GW2 +13, WC GW8 (11-player rebuild), TC GW13 +2 and TC GW26 +7 (sound ~8-10 EV picks, captain variance both times), BB GW19 +16, **FH GW33 +47** (the advisor's EV spiked to +19.3 on a blank week and the realized rescue tripled it), BB GW36 +16, WC GW38 force-played. Measured +85 sits inside the +60-120 hand-wave used for the rank estimate; 2,237 against the season's known anchor (2,324 → rank 19,100) puts the full engine loop around the top ~0.5% — before the team-news advantage the live product adds.

Wiring mirrors the planner: `chip_advice` replaces the last honest stub in the tool surface (`_resolve_squad`/`_gw_projections` now shared with `plan_transfers`), `POST /chips/advise` behind auth, the chat tool with per-chip assessments (grounding evals updated — the chip case now demands engine routing or asking for the 15; 8/8 live), a chat ToolCard, and a Planner "Chip timing" panel: per chip the best week, expected gain, assessment, and a per-GW EV bar strip. ChipsCard's copy now points at the Planner instead of promising a future feature. `next build` clean; signed-in visual pass still manual.

## 26. Platform gates — CI, a daily chat spend ceiling, and played-GW ingest

The remaining pre-GW1 platform items (TODO §1) plus the first §2 deadline item, landed together.

**CI.** `.github/workflows/ci.yml` is the always-on gate, a few minutes per run: ruff check + `format --check`, a new data-free pytest suite, and `next build` with the dummy Clerk publishable key. The suite (`tests/`, now the pytest testpath) runs with no parquets, no network, and no credentials by design: season-config validation, canonical-schema invariants, `build_future_rows` and the whole played-GW transform driven by synthetic API payloads (`tests/synthetic.py` mirrors real endpoint shapes down to the floats-as-strings), the chat budget, and `/health` over an empty volume. The grounding evals spend real Anthropic money, so they live in `grounding-evals.yml`, path-gated to `api/chat.py`, `api/tools.py`, and the eval suite (plus `workflow_dispatch`); that job seeds a throwaway data volume exactly like a fresh deploy (historical download cached by Actions → feature builds → live snapshot → pipeline, the committed ratings-weights cache sparing the replay refit) and fails loudly if the `ANTHROPIC_API_KEY` repo secret is missing — **the secret still needs to be added in GitHub settings**, and a run during the FPL API's maintenance window fails at the snapshot step (re-run later).

**Chat daily spend ceiling.** Alongside the per-user hourly limit, `/chat` now enforces `CHAT_DAILY_TOKEN_CAP` across *all* users: every token the tool runner reports (input, output, cache writes and reads) lands in an in-memory UTC-day ledger in `api/chat.py` (same single-process posture as the rate limiter), and an exhausted budget 429s with the honest "daily chat budget spent — resets at midnight UTC" before any Anthropic call. Unset means no ceiling; an unparseable value fails *closed* — a typo'd cap must not mean unlimited spend. TestClient-verified: cap 0 blocks immediately, a spent ledger blocks, the ledger counts all four usage fields and prunes itself at the day boundary.

**Played-GW ingest** (`engine/ingest/played_gw.py`, the GW2 deadline item). The job gates on events that are `finished` *and* `data_checked` (no provisional bonus) and, within them, finished fixtures not yet in the canonical table — so pre-season is a verified clean no-op, re-runs never duplicate, and a postponement is picked up whenever its fixture eventually plays, because ingestion is keyed on fixtures, not events. Source choice that mattered: `event/{gw}/live` aggregates a DGW into one stats block, so the per-fixture rows come from `element-summary` histories (one entry per fixture — DGW-safe by construction) fetched for the full pool and archived as one combined snapshot next to the per-GW live payloads. Three validations run before anything is appended: `defensive_contribution` must equal the raw CBIT/CBIRT count exactly (cbi+tackles for DEF, +recoveries for MID/FWD — re-verified against all 29,747 2025-26 archive rows; GKP has no exact identity and is skipped), per-player GW totals must reproduce the live endpoint's `total_points`, and the append casts to the existing table's dtypes so schema identity with the archive holds by construction (a dtype-drift guard test pins the recorded schema against the real parquet). After appending it rebuilds the matches table (with a staleness self-heal on the next run if that step crashes), so team strength trains on the new scores; promoted-team names are aliased to archive identities at ingest time ("Hull City" → "Hull") to keep one club identity across seasons.

Wiring: a nightly APScheduler job at 01:15 UTC — before the 02:30 pipeline, so the night after a GW wraps retrains on it — plus a seed-time catch-up so a mid-season redeploy backfills before its first pipeline run, same graceful-degradation posture as the other jobs. The pipeline needed two changes to treat appended rows as history: `LIVE_SEASON`/`TEAM_ALIASES` moved to the ingest module (pipeline re-exports), and the per-GW projection loop now selects each GW's *future fixtures* explicitly rather than `season == live` — with played live-season rows in the archive, that equivalence is gone, and the explicit filter keeps phantom-row hygiene intact while the models happily consume the played rows as history. The projection window rolls forward on its own (`is_next` from bootstrap).

**Verification.** 19/19 tests (schema identity, idempotency, partial-GW pickup, DC-points-shape rejection, live-totals mismatch rejection, AM dropping, aliasing, cap behavior); an end-to-end acceptance run on a scratch volume — synthetic played GW1 built from the real snapshots, 555 rows appended twice (no dupes, dtypes byte-identical to the archive), matches +10, full pipeline → window rolled to GW2-7 with zero played-fixture leakage; the real orchestrator against the live API → clean pre-season no-op; the real pipeline end-to-end unchanged; grounding evals 8/8 live (the usage ledger sits inside the tool-runner loop, so this also exercised it); `next build` clean (568 pages). Holdout evals reproduce by construction: no model module changed and the real archive is byte-identical (the eval paths filter `season < holdout`, so even in-season appended rows can't reach them).

## 27. Real team import — the planner and chip advisor get their live inputs

TODO §2's team import, built pre-deadline against mocked payloads and armed to go live the moment FPL makes picks public (GW1 deadline night). `engine/ingest/team_state.py` turns a team ID into exactly the state the optimizers need, from four public endpoints (`entry`, `history`, `{gw}/picks`, `transfers` — `entry_transfers` added to the API client). Two reconstructions the public API doesn't hand over: **free transfers** are walked forward from the manager's first GW with the banking rule (`f' = min(5, max(f−n,0)+1)`, wildcard/free-hit weeks consuming nothing — the same linearization the planner MILP uses), and **purchase prices** (the sell-price rule needs them; only the authenticated my-team endpoint exposes them) come from season-start prices (`now_cost − cost_change_start`) plus the transfer history's exact `element_in_cost`, skipping free-hit weeks because those transfers revert. Sell price = purchase + half the profit rounded down — and since `optimize_transfers` already took `squad` as code → *sell value*, the whole thing wires in without touching the MILP. Post-deadline transfers pending for the next GW are applied to the squad and bank (free-hit pendings excluded); managers who joined after GW1 get current-price fallbacks with an explicit `approx_purchase_prices` warning instead of silent wrong numbers. Pre-deadline, the module returns an honest `status: "pending"` payload — verified live against the real API (entry 1 → pending with the explanation; absurd ID → clean 404).

Surface wiring: `GET /team/{id}` (public data, no auth, 5-min TTL cache — one import is 4-5 FPL calls), `team_id` as an alternative input on `/transfers/plan` and `/chips/advise` (squad resolved by stable player *code*, not name matching; the chip advisor auto-restricts to chips actually in hand and refuses politely when none are left this half), and an `import_team` chat tool with its own typed card. **Grounding evals now 9/9 live** — the new case shares a nonexistent team ID and requires import_team routing plus zero invented numbers. UI: My Team prefers the imported board when the saved ID resolves — FPL's own XI/captain/bench order rather than the optimizer's suggestion, with bank/value/FT/rank in the This Week panel, real chip state (played GWs / in hand / next half) on the ChipsCard, and the pending/error states on the onboarding card; the Planner switches its plan and chip calls to `team_id` and says whose money it's planning with. Draft flow untouched for everyone else.

Tests: 13 new (32 total) — the FT walk (hits, hoarding cap, chip weeks), purchase reconstruction (season-start vs transfer-in vs free-hit-skipped vs late-joiner-approx), sell-price rule, chip halves across the GW19 split, full state assembly with a pending transfer, fetch orchestration against a fake API (pending/404/cache), and the REST endpoint with store and fetch faked so CI stays data-free. `next build` clean. Deliberately deferred: the real-board visual pass (no public picks exist anywhere until deadline night — the flip-on moment is the verification) and one-line advisor recs on the chips card (a status card shouldn't run an 8-20 s MILP; the Planner is one tap away).

## 28. Multi-season backtest harness — the pre-launch quality debt, paid down

The accepted debt from PLAN §9 (one-season validation instead of three) is retired. `backtest/harness.py` replays 2023-24, 2024-25, and 2025-26 point-in-time — minutes and event-rate models fitted strictly on earlier seasons, team strength refit before every GW, and *per-season scoring rules* via new historical configs (`engine/config/seasons/2023-24.json` etc.: no defensive-contribution points before 2025-26 — replaying old seasons under today's rules would have biased xPts against realized points). The engineering trick that makes tuning affordable: the expensive per-season work (minutes model, 38 as-of team-strength fits) doesn't depend on the rate half-life, so it's cached once per season and the sweep re-runs only rates + assembly — the full 3-season × 4-half-life grid plus weights refit is a single ~40-minute run.

**Headline: the engine's edge generalizes.** On every season, every decision metric beats both naive baselines (last-4 form, season PPG; FPL's `ep_next` doesn't exist in the historical archive): played-rows rank correlation 0.357 / 0.358 / 0.370 vs form's 0.274-0.282 and PPG's 0.290-0.313; RMSE 1.90-1.92 vs 2.04-2.15; top-10 overlap 0.32-0.34 vs 0.29-0.31; total xPts within +0.6% to +3.4% of realized; captain picks beat the form-captain in all three seasons. 2025-26 was not a fluke season, and the pre-launch numbers reproduce.

**Rate-decay half-life: 270 → 180 days.** The sweep's first finding is a robustness result — RMSE, rank correlation, and top-10 are flat to three decimals across 120/180/270/365, because empirical-Bayes shrinkage absorbs most of the decay choice. The metric that moves is the one the Salah miss lived in: captain quality. 180 wins the aggregate (6.84 vs 6.42 pts/GW across the three seasons) and the decline-season case specifically (2025-26: 6.11 vs 5.42 — roughly +25 armband points over a season), with nothing degrading anywhere. Caveat recorded honestly: 38 captain picks per season is a noisy sample and 2023-24 marginally preferred the longer decay; the aggregate and the mechanism (faster tracking of aging/declining rates) carried the decision. Live projections rebuilt under 180.

**Cross-season weights refit: run, validated, and rejected.** NNLS on pooled 2023-24 + 2024-25 windows validated *worse* on held-out 2025-26 than the currently cached 2025-26-fit weights (rank corr 0.536 vs 0.572). The cause is structural, not statistical: two of three seasons predate defensive-contribution scoring, so the DC-floor sub-score is degenerate in their windows — the rating problem changed regime in 2025-26, and pre-DC seasons are off-distribution for fitting it. The cached weights stay; the refit becomes worthwhile once 2026-27 accumulates in-season windows via played-GW ingest. This is the harness working as designed — a plausible "improvement" caught by its own holdout before it shipped.

Still open, now with numbers attached: the captain-vs-hindsight gap (≈6-8 realized pts/GW vs ≈17-18 for the perfect armband) is the ceiling-aware-captaincy headroom, and the planner's γ=0.9 remains a single-season fit (a proper revisit needs per-season frozen replays + planner sims — deferred as compute-heavy). Suite: 4 new CI-safe tests (36 total); results archived to `data/backtest/harness_results.json`.

## 29. Live accuracy monitoring — the credibility surface goes public

The last item in TODO §2. The problem it solves is structural: the nightly pipeline overwrites `data/live/` in place, so "how good were the projections?" was unanswerable the moment a gameweek kicked off. Now `engine/accuracy.py` owns a point-in-time record with one honest rule at its core: after every pipeline run it writes each visible GW's per-fixture projections to `data/accuracy/frozen/gw{N}.parquet` — **and refuses to touch that file once GW N's deadline has passed**. Pre-deadline runs overwrite freely, so what survives a deadline is exactly what the site was serving when it hit. The freeze also captures FPL's own `ep_next` per player *from the same bootstrap snapshot*, which finally gives the live season the baseline the backtests never had (it doesn't exist in the historical archive) — the engine's public claim is now measured against FPL's own projection, frozen at the same instant, scored the same way.

Scoring runs after every played-GW ingest and rescores *everything* from the frozen files (cheap, idempotent, self-heals postponement top-ups): join to realized rows on (gw, element, fixture) — fixtures postponed out of a GW never match and are disclosed as `fixtures_scored < fixtures_frozen`, never silently dropped — collapse to player level (DGWs sum), then the PLAN §8 suite per GW against `ep_next` and a live last-4-form baseline: RMSE/MAE, within-position Spearman on players who played, top-10 overlap, captain pick vs hindsight, clean-sheet + start Briers, xPts/actual ratio. One subtlety that matters for honesty: `ep_next` is only a fair baseline for the GW that was *next* at freeze time, so each frozen file records `ep_next_event` and the scorer refuses the comparison when the horizons don't line up (back-to-back midweek edge). Output: `accuracy.json` (per-GW entries + pending freezes + season aggregate) and `player_detail.parquet`, served by two new **public, unauthenticated** endpoints — `GET /accuracy` and `GET /accuracy/{gw}` — because a track record behind a login isn't one.

The site gets a public `/accuracy` page (nav, sitemap, the full four-file API trail): season tiles (rank quality vs `ep_next`, beat-FPL count, captain pts/GW vs hindsight, RMSE), a per-GW model-vs-FPL bar comparison (two-series palette validated with the dataviz six-checks: royal + the cs-blue token; slate failed the chroma floor), the per-GW table with postponement disclosure, and a per-player projected-vs-realized scatter with position/played filters, hover provenance, and direct labels on the biggest beat and miss. Pre-season it renders an honest empty state — "the ledger opens after GW1", listing what's already frozen and when each GW locks — instead of pretending. Verified both ways: 7 new CI-safe tests (43 total; deadline gating, DGW collapse, postponement disclosure, `ep_next` horizon refusal, form4 point-in-time, idempotent rescore), a real freeze against the live volume (GW1–6, 563 players × 10 fixtures each, `ep_next` fully populated), and a browser pass on both page states via a synthetic scored season pointed at `FPL_ACCURACY_DIR` — which caught two real bugs: a `flex-1`-in-column bar collapse (flex-basis 0 beats `h-2.5`) and a locale-dependent deadline format that hydration-mismatched (fixed with a pinned en-GB/UK-time formatter; note: `lib/format.formatDeadline` has the same latent mismatch on My Team for non-en-US visitors). First real scorecard appears automatically the night GW1 goes data-checked (~24 Aug) — freeze tonight onward needs no action.

## 30. Noted, not fixed: 2026/27 BPS changes put a known bias in the bonus layer

FPL changed the Bonus Points System formula for 2026/27 (premierleague.com "What's new" series, 28 Jul 2026): the −1 BPS for being tackled is gone (helps dribble-heavy wingers/full-backs), CBI drops from 1 BPS per 2 to 1 per 3 (an explicit centre-back nerf), and GK saves are restructured (all saves 2 BPS, +1 inside the box, +1 for a new "big chance save" metric; penalty saves 8→7). Bonus is still 3/2/1 and **no points-table values changed** — the scoring config and the played-GW DC validation guard are untouched.

Where this lands in the engine: the bonus layer is a per-position BPS regression + per-player `bps_res90` "bonus magnetism" residual + Plackett-Luce (`points.py` / `event_rates.py`), all fitted on old-formula historical `bps`. The saves/penalties-saved regression coefficients are now mis-specified directly; CBI and tackled aren't regression features, so their changes land in the *residuals* — meaning the bias is player-shaped: **centre-back bonus projections will run hot early season, dribbler bonus cold, GKs re-shuffled**. Magnitude is second-order (~0.2–0.7 xPts at the bonus-magnet end) but it's the margin that orders premium captain picks and the DEF "bonus" sub-score. Decision: no pre-emptive refit. A backcast is only partially computable anyway (the archive has `cbi`, so `floor(cbi/3) − floor(cbi/2)` could adjust history; being-tackled counts and box/big-chance save splits exist nowhere in the archive or API history), and `bps_res` carries a 15-effective-90s shrinkage prior, so nightly retraining on ingested 2026-27 rows corrects it gradually. The accuracy page (§29) is the watch instrument — if early-season rank correlation or the xPts/actual ratio lags `ep_next` beyond noise, revisit the CBI backcast and consider re-fitting the BPS regression on 2026-27 rows only once a few GWs exist. Related, from the same article series: FPL now ships a native price-change predictor (progress %, 15-min updates, live after the GW1 deadline) — which makes TODO §3's price-change model a candidate for reframing from *build a predictor* to *ingest and surface FPL's own*.

## 31. Prior-vs-observed provenance badge — the projection admits what it's made of

TODO §3's cheap grounding win, timed for when it matters most: pre-season, when deadline-day signings are pure price-tier priors and peak traffic meets minimum data. `engine/models/event_rates.data_basis()` converts a player's time-decayed `exposure_90` into the **exact** empirical-Bayes blend the model actually uses — `prior_weight = k/(exposure + k)` at the attacking strength k=10 — plus a four-level label: `pure_prior` (no PL data in the archive: new signings, promoted-club players — no Championship data exists), `mostly_prior` (weight ≥ 0.5), `mixed` (≥ 0.25), `observed`. Not an invented heuristic; the badge *is* the shrinkage equation.

Wiring: `data_basis` on every player-shaped tool payload (`get_player`/`compare_players` via `_player_summary`, `explain_rating`), a compact level flag on `rank_players` rows only when prior-heavy (lean payloads, but a pure-prior newcomer can't pass as an observed-data pick), `{level, effective_90s}` per player on `/explorer`, and a grounding-contract line in the chat system prompt so Pal warns before recommending a prior-heavy player (chat.py change → the paid grounding evals run on push, first time under the newly added repo secret). UI: a `BasisBadge` pill in the player drawer and profile header for pure/mostly-prior, and a `BasisDot` in the explorer tables. One real-data calibration decision: pre-season the pool splits 90 pure-prior / 298 mostly-prior / 175 mixed / **0 observed** (a full season decays to ~20 effective 90s under the 180-day half-life — honest, but it means "mostly prior" describes half the league in July), so the dense-table dot marks pure-prior only; the drawer/profile badge covers both. Verified on the live volume and in the browser (Victor Munoz, a £6.5m pure-prior signing: badge, dot, profile, and rating payload all carry it); 44 tests, build clean.

## 32. Monitoring — the site can now tell someone when it breaks

The launch-blocking operational gap (TODO §5): jobs ran unattended nightly and nothing would notice a failure. Two halves, both env-gated no-ops without their keys, so local dev and CI are untouched.

**Errors: Sentry on both apps.** Engine side is four lines in `api/app.py` — the FastAPI integration reports unhandled 500s, and the default logging integration turns the jobs' existing `log.exception(...)` calls (plus APScheduler's own job-crash logs) into issues, so the nightly pipeline/ingest/freeze/score chain can't die silently. Errors only: `traces_sample_rate=0`, no PII. Web side is `@sentry/nextjs` in its minimal shape — `instrumentation-client.ts`, server/edge configs, `global-error.tsx` (render crashes get reported *and* users get an honest reload shell instead of a white page), `withSentryConfig` with source-map upload only when a token exists. First build came in at +85 kB shared client JS, which was unacceptable; `bundleSizeOptimizations` (strip tracing/replay/debug paths) got it to +33 kB — the class of bug it catches (the `formatDeadline` hydration mismatch was found by accident) justifies that.

**Uptime *and freshness*: `/health` got the design insight.** A bare 200 check misses this system's scariest failure — the pipeline dying while the API happily serves yesterday's projections. `/health` now reports `snapshot_age_hours` (last archived bootstrap) and `computed_age_hours` (provenance), and a single `healthy` flag that goes false past honest thresholds: snapshot >6h (hourly job, with grace for FPL's own maintenance windows) or computed >26h (nightly pipeline). A free UptimeRobot keyword monitor asserting `"healthy":true` therefore catches process-down *and* silently-stale, distinguishably (the endpoint stays 200 when stale). Verified live by accident: the local volume hadn't refreshed in 3 days and `/health` promptly said so. 7 new tests (51 total; threshold boundaries, maintenance grace, endpoint with faked store); DEPLOY §2.5 documents the ~15-minute account setup (two Sentry projects, two monitors) that remains manual.

## 33. Horizons split by decision — the XI is a weekly call, the armband is not

Two suggested improvements, one shipped and one rejected by its own experiment. Both started from the same real defect: `api/data.py` collapsed the projection window into a single summed `xpts`, and `optimize_squad` used that one column for three decisions with different natural horizons — which 15 to own, which 11 to start, and who to captain.

**Shipped: the XI now solves on the upcoming GW.** Which 15 to own is a horizon decision; which 11 to start is not — you re-pick the XI every week, so the window has no business in it. `LiveStore` gains an `xpts_next` column (upcoming GW, DGW rows summed) and `_relineup` re-solves the XI and bench order on it inside the already-chosen 15. The squad MILP is untouched, so the 15 is provably unchanged. Worth **+0.38 realized pts/GW (+14/season), SE 0.76** on the frozen 2025-26 replay — the right shape, statistically indistinguishable from zero. Shipped on correctness, not on the points.

**Rejected: captaining on the upcoming GW.** The same reasoning appears to apply to the armband, and it does not. On 37 frozen 2025-26 decisions, a next-GW captain scored **5.05 pts/GW vs 6.11 for the 6-GW rule (−39/season)**; 1 GW was the worst of every horizon from 1 to 8, blending was flat for weights 0.2–0.6 and collapsed above 0.7, and splitting out DGW/blank weeks didn't rescue it. The mechanism is the max operator: captaincy is an argmax over 15 candidates, so it rewards whoever drew the luckiest single-week estimate, and the horizon sum is doing implicit shrinkage toward player quality. Concretely it swapped Haaland out for Bruno in GW14 (−10), Bruno for Igor Thiago in GW16 (−11), Haaland for Palmer in GW33 (−11). Picking a top-11 *set* averages that noise out rather than amplifying it — which is exactly why the XI wants the opposite horizon to the armband.

That asymmetry drove the implementation. The first attempt ranked the captain on the horizon but *valued* it there inside a per-week objective, which over-rewarded captaining a player about to blank (a synthetic test caught it). The shipped version keeps the metrics in separate steps: the horizon leader takes the armband, is forced into the XI (FPL won't let you captain a substitute), and only the other ten slots are optimized on the week. One deliberate exception — a leader with no fixture is skipped, since a blanking captain scores nothing.

Verification: the 15 and the captain are identical in **37/37** replayed gameweeks, ~1.1 XI swaps per GW, solve time unchanged. `xpts_next` is optional, so every engine caller that passes an explicit column list (`transfers.py`, `chips.py`, the backtest sims) is untouched by construction. Payloads gain `xpts_this_gw` / `xi_plus_captain_xpts_this_gw` and a `horizons` note so the chat layer can't conflate them; the My Team board headline was mixing the two — a real team's figure came from next-GW xP while a draft's came from the window — and now consistently shows the upcoming GW. 6 new tests (57 total).

**Follow-up worth more than either suggestion:** `transfers.py` already captains per-gameweek on that GW's xP — it is doing the thing this experiment just rejected, inside the joint objective where it may also skew transfer choices. Testing a shrunk captain term there needs a `planner_sim` re-run. Caveats on all of the above: one season, 37 decisions, news-blind. Ceiling-aware captaincy (TODO §3) is still untested because `var_pts` isn't in the cached frozen replay.

---

*Remaining pre-launch: the Sentry/UptimeRobot account setup (DEPLOY §2.5), the deadline-night live check of team import, and — recommended before GW1 — an off-site backup of the volume's irreplaceable `data/accuracy/frozen` + `data/snapshots`. UI backlog per UI_PLAN §12: dark mode, live mode, drag-and-drop, comparison pinning, ownership overlay.*
